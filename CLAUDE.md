# Kyle Tran — Portfolio · Design & Working Guidelines

Guidelines for designing/editing this website. Claude Code auto-loads this file, so treat it
as the source of truth for how this site is built and how to keep it consistent.

---

## 0. TL;DR for a new session
- **One file:** `index.html` (HTML + inline `<style>` + two inline `<script>`s). **No build step, no framework, no npm.** Edit it directly.
- **Preserve the engine:** SPA hash router, theme toggle, lazy image loaders, video facade, scroll-reveal, count-ups, and the **dev-mode editor**. Don't rip these out.
- **Design law:** near-monochrome + **ONE** functional Northeastern-red accent (`--signal`). No gradients. No second color. Accent is for *signals only*, never decoration.
- **Light is the default theme** for every visitor (owner, 2026-09-23); dark is on the toggle. Three places decide it and must agree: `<html data-theme>`, the boot script in `<head>`, and the main script's theme block.
- **The landing's first screen is the foam case** (§4). The owner kept that one screen and rejected the motif everywhere else, so don't spread it to other sections or pages.
- **Readability:** body text ≥ 16px, mono labels ≥ 12px. Never smaller.
- **Verify visually before finishing** any visual change (headless screenshot QA — see §9). The automated audit won't catch bad *taste*; look at the screenshots.
- **Images:** reference only the optimized copies in `images/web/`. Optimize new photos with Pillow (`exif_transpose`!). Never reference the big originals in `images/`.
- **Don't fabricate** specs/numbers/materials. Use the owner's words and the GitHub repos.
- **Encoding trap:** never round-trip `index.html` through PowerShell `Get-Content`. Use the Edit tool or Python (utf-8). See §7.

---

## 1. What this is
- Personal portfolio of **Kyle Tran**, Mechanical Engineering @ Northeastern. Hardware/robotics focus.
- **Single-file static site**, deployed on **GitHub Pages from `main`** (repo `ka1e27/Portfolio`). Pushing to `main` deploys.

## 2. Aesthetic direction
**"Field-instrument editorial"** — precise, technical, datasheet-like, warmed by an editorial serif. Light-first: light is the default theme (owner, 2026-09-23), dark is on the toggle.
- **Type (do not change):** Fraunces (display serif — use its `ital` + `opsz` axes), Manrope (body sans), IBM Plex Mono (data / labels / captions / metadata).
- **Color:** near-monochrome + one accent `--signal` — **Northeastern red** (changed from hazard-orange 2026-09). Use the accent ONLY for: status dots, REV/NEW markers, result markers, focus rings, hover cues, ranking chips, the timeline rule. Never a brand wash, never a gradient, never a 2nd hue.
  - **The red is per-theme on purpose.** Northeastern's brand red `#C8102E` makes 5.21:1 on the light cream `#F3F1EC` (AA, use it as-is) but only **3.31:1 on the dark `#0D0D0C`** — it fails AA for the 12px mono labels the accent is mostly used on. So dark uses it lightened to `#E63C52` (4.75:1). Light `#C8102E` / dark `#E63C52`; `--signal-strong` is `#A50D26` light (darker) and `#FF5068` dark (brighter). **Recompute contrast if you retune either** — don't just pick a red you like.
- Keep the **film-grain** overlay and the **fully-tokenized dark + light themes** (light is authored, not a filter-invert).

## 3. Tokens (defined in `:root` / `[data-theme]`)
- **Spacing:** `--s1`…`--s11` (4→184px). Use these; don't hardcode px for layout gaps.
- **Type scale:** `--fs-hero`…`--fs-100` (fluid `clamp()`). Label floor `--fs-label` ≈ 12.5px, `--fs-label-sm` = 12px. Body floor `--fs-400` = 16px. `--fs-300` (14.4px) is for *secondary/meta only*.
- **Radii:** `--r-pill` (nav/buttons/tags) · `--r-panel`/`--r-chrome` (panels) · `--r-frame` (media, near-sharp 3px). **Keep this split** — do NOT uniformize corner radius (that's an AI-generic tell).
- **Portrait caps:** `--cap-sm/md/lg` (340/460/580) — apply by role so tall images don't blow up.
- **⚠ `ch` gotcha:** `ch` is relative to the element's *own* font-size. Don't put a `ch` max-width on a wrapper whose children use a much larger font. (A `22ch` on the project title *block* measured against the 16px body font = ~200px and squished every project header — that bug is fixed; don't reintroduce it. Put the measure on `.pd-title` itself, which uses the big font: `max-width:16ch`.)
- **⚠ Nested-wrapper gutters:** case studies put a `.container` inside `.container-wide`. Both used to pad, so phone text sat 44px in from each edge (a 302px column on a 390 screen). A rule now drops the inner padding and caps the inner `.container` at `--maxw` minus two gutters, which lines it up with `.pd-nav` and the chapter summaries. Don't give a nested wrapper its own gutter back.

## 4. Layout & components (reuse, don't reinvent)
- Wrappers: `.container` (1240) · `.container-wide` (1500) · `.full-bleed` (100vw). Reserve **one** frameless `.pd-bleed` image per case study as a deliberate break.
- **Home order (recruiter skim path):** foam-case hero → `.cred-strip` → marquee → work grid → contact. The stats strip sits **directly under the hero** so the proof lands in the first screen at desktop (still true at 1440×900 with the case); it used to sit after all five projects, where most visitors never scrolled. Keep the proof high.
- **Foam case (the landing's first screen, 2026-09-23).** The work shown as objects in a tool case: each project in foam cut to its outline, with a red floor (`#B00E26`) showing through each cut (two-layer "shadow foam", from 5S). The name, lede and buttons sit on the left, with the class year and the co-op as label-maker tape (`.dymo`) and `.sf-facts` under them. The case sits on the right. It stacks at ≤1100px and the pockets go 2-up at ≤640px.
  - **Markup:** `.sf-hero` > `.sf-id` · `.sf-case` (`.sf-insert` > `.sf-pockets` > six `.sf-pocket`) · `.sf-facts`. A cutout pocket stacks two images on one canvas: `.sf-pk` (the cut, with its floor and shadow) under `.sf-ob` (the object). An `.sf-slot` pocket holds a photo card instead. Every pocket carries `data-goto`, so the router opens its case study; on hover the object lifts out and its tape turns red.
  - **Styles:** one self-contained `<style id="foam-hero">` at the end of `<head>`, scoped to `.sf-hero`. The foam follows the theme: warm white `#F6F5F1` in light and `#121211` in dark. Light foam also gets a dark hairline on each cut and a dark grain, because the rim baked into the pocket images is a light one, drawn for black foam. Its theme values come after its `:root` block on purpose: at equal specificity a later `:root` would override them.
  - **Assets:** `images/web/foam/` (object + `-pocket` PNG pairs, `boat-slot.jpg`, `combat-slot.jpg`), all generated by `tools/foam_assets.py` from the site's own renders and photos. Rerun it rather than hand-editing the PNGs. It also writes `images/web/dreams-v1-dock-wide.jpg`, the boat case study's hero crop (2:1, so its first screen shows the boat, not the marina).
  - **Scope:** first screen only. The owner reviewed the motif applied site-wide (drawers of photos, foam case-study figures, tape labels everywhere, nameplates, drawer handles) and rejected it. The rest of the site is the original design.
- **Work grid:** featured card at 2:1 (16:10 at ≤820) + bento (`.col-7` / `.col-5` / `.col-12`). A row's two cards share one fixed media **height**, not separate aspect ratios (those left the pair 20px apart), and the meta line is pinned to the card bottom so dates and "View" cues sit level.
- **Project detail:** `.pd-kicker` (one line) · `.pd-title` (`max-width:16ch`) · `.pd-tags` · `.pd-nav` (sticky jump buttons; every page over ~4 screens gets one, and each target needs an `id`) · `.spec-strip` (datasheet grid; add `.cols-2`) · `.pd-result` · `.pd-links` (CAD / GitHub / live-app pills) · `.pd-split` (asymmetric 5fr/7fr; `.media-left` flips it; `.is-portrait` caps the **figure**, so the caption starts where the photo does; `.is-pair` makes two photos equal columns) · `.pd-sticky` (pinned media + build steps) · `.diff-table` (v1→v2 with `.rev` markers; always wrap in `.diff-wrap` so it scrolls instead of widening the page) · `.spec-details` (BOM `<details>`) · `.pd-gallery` (masonry, *order-agnostic* sets; `.cols-2` when one tall portrait would strand a 3rd column) · `.build-strip` (horizontal snap, *chronological* sets, grayscale process photos) · `.fig-annotated` (CAD callouts; `data-target` on a `.callout` highlights `.spec-cell[data-spec=…]`).
- **Figures:** `.img-fig` > `.img-el` (never cropped) + `.img-cap` (auto `FIG.NN` via CSS counter scoped to `.pd`). Cards use `.cover` + `data-img` (cropped thumbnail contract — different from figures on purpose).
- **Drawn figures, for things not built yet** (arm page): `.kin-fig` (inline-SVG joint schematic) and `.flow-fig` (HTML load-path / sequence block), both on a faint drafting grid built from `--grid-line-strong` so they follow the theme. Draw **only what the adjacent text states** (joint order and type, load path) and caption it "schematic, not to scale". Prefer this to a dashed "coming soon" `.fig-ph` box; recruiters read an empty frame as unfinished work. Swap in the real CAD once it exists.
- **Problem → fix pairs** (maze page): a `.pd-split` whose text is a `.pd-sticky-step` ("FIX 01 — TREADS", an h3, a paragraph) and whose media is `.fig-stack.is-row`, two 3:4 photos side by side, stacking below 480px. Put the text **first** in the markup: `.media-left` put the photos above their own heading once the split stacks at ≤820px.
- **Code figure** (`figure.img-fig.code-fig > pre > code`): a real firmware excerpt, monochrome, and it scrolls inside its own box. Keep `min-width:0` on it, or its longest line widens the grid track and the whole page scrolls sideways on a phone. Quote the repo's code as it is; trimming lines is fine if the caption says "excerpted".
- **Combat robot showcase:** `.robot-rank` chip + `.robot-lead` + body + CAD link.
- `.ig-feature` (Instagram feed block) · `.maker-cta` (MakerWorld) · `.cred-strip` (home stats) · `.marquee` (tool list). The old hero's `.hero`, `.hero-telemetry` and `.hero-motif` rules are still in the main stylesheet but unused since the foam case replaced that hero.
- **Nav:** `.nav-resume` is an outlined pill in the header bar, the one link a recruiter wants from every page. In the phone menu it goes full-width, and tapping it closes the menu (it opens a new tab, so the router never runs).
- **Contact band:** `.contact-email` shows the address as selectable text, with a `.contact-copy` button (Clipboard API, then a textarea fallback, then select-the-text, announced through `#copy-status`). A mailto button alone does nothing for webmail users. Keep the `<wbr>` after the `@`, which gives it a clean break point if it ever has to wrap. The "Save as PDF" brief is a quiet text link (`.contact-pdf`), not a fifth pill. On phones the hero and contact buttons are a 2-column grid with the primary spanning the row; don't let a lone pill wrap onto its own line.
- **Lightbox:** sequence viewer (prev/next, counter, `←/→/Esc`, swipe, focus-trap) bound to any `.img-clickable`.

## 5. Motion (all reduced-motion-safe; `@supports`-gate scroll-driven CSS)
- Keep: View Transitions on tab switch, `.reveal` choreography (`data-reveal` direction + `data-reveal-delay`), native `animation-timeline: view()` "develop" on covers, magnetic primary buttons (`(hover:hover) and (pointer:fine)` + `!reduce` only), variable-font nav weight (`@property --wght`), count-ups, scroll-progress bar, tool marquee, the foam case's lift-out on hover. (The contour-motif parallax went with the old hero; its code is null-guarded and now inert.)
- **Always gate JS motion behind `!reduce` at the source** (don't allocate rAF loops for reduced-motion users). Confine spring easing to small interactive chrome, never page-level moves.
- **Avoid (2026 cringe/AI-slop):** scroll-jacking, full-page smooth-scroll libraries, cursor *replacement*, uniform fade-up-blur on everything, per-character text stagger, heavy WebGL, big tilt + gloss, contentless marquees, reveals that re-fire on scroll-up.

## 6. Content & accuracy rules
- **Never fabricate** specs, numbers, materials, rankings, or process claims. Source from the owner's descriptions and the GitHub repos: `ka1e27/D.R.E.A.M.S-V1`, `ka1e27/Autonomous-Maze-Navigation-Robot`, `ka1e27/D.R.E.A.M.S-Vision-Test`.
- **Known-true facts (keep consistent):** GPA **3.87**; **10** robots designed; **16** podium finishes across 5 events; **4th** national Plastic Antweight (2024 RCL). He has **run 2.5D CNC himself** (owner, 2026-09-23); the combat robots' machining and heat treating were **outsourced**. Keep the two apart wherever machining comes up. Dog is **Mocha**. No "hammer" weapons.
  - **Whose podiums (owner, 2026-09-23):** the awards table is **his own robots** (4 firsts, 8 seconds, and the 4th-place national). The **16** ("4 firsts, 7 seconds, 5 thirds", across 5 events) is the **club's**. Say whose wherever a count appears: the home strip ("Team podium finishes"), the combat card and the combat case study's result ("as a club"), the print brief, the President entry, and the note above the awards table. Both are right (owner, 2026-09-23: "the website is right"). They don't conflict: the awards table covers 10 competitions (11 events, with two NorCal Robotics Expos), while the club's 16 counts 5 events. Don't "reconcile" them.
  - **Graduating May 2029**; started Sep 2025; **seeking a Summer 2027 co-op** (hardware / robotics). Confirmed by the owner 2026-09. These must stay in sync everywhere they appear. The co-op is in the hero's red tape label ("Seeking · Summer 2027 co-op"), the contact lead, the print brief's `.ps-lead`, `<meta name="description">`, `og:description` and the résumé's headline (`tools/resume.html`). The grad date is in the About "Graduating" cell, the Education card date, the brief's `.ps-when` and the résumé's Education line. The **class year ('29)** is in the hero's black tape label ("Mech. Eng. · Robotics minor · NU '29"), the brief's `.ps-role`, the résumé's headline and, as pixels, in `images/web/og-card.jpg`.
  - **The résumé and the brief tell one story (2026-09-23).** `Kyle_Tran_Resume.pdf` is printed from `tools/resume.html` (FlowCV is no longer its source) and the two-page brief from `.pdf-sheet` in `index.html`, both with `tools/print-pdf.mjs`. Same projects in the same order (boat, combat, arm, maze), the same dates, and the same numbers: his robots' record as "4 first- and 8 second-place regional finishes" and the 16 as the club's. "2.5D CNC machining" as a skill, and "sent out" for the combat robots' frames. Change one and check the other; `tools/README.md` has the commands and the two layout traps.
  - **`og-card.jpg` is an image of text** (name, "Mechanical Eng. · Robotics · Northeastern '29", 4th / 16 team podiums / 3.87 GPA). If any of those change, re-render it from `tools/og-card.html` (see `tools/README.md`), or LinkedIn/Slack previews will keep showing the old numbers.
  - **Boat (D.R.E.A.M.S):** v1 = 3× RP2040 (boat/shore/pod), HC-12 433 MHz half-duplex, PLA hull, winch bottom-contact sampling, JSON missions + CSV + waypoint-planner web app (`dreamsv1.kylegtran2007.workers.dev`). v2 = Pi 5 + 2× RP2040, stereo-vision avoidance (YOLOv8 gated by anomaly screen), VESC diff-drive, 4G→PostGIS cloud, power = M18 for electronics + winch with a 12S LiPo drive planned, **ASA hull with fiberglass + epoxy skin**, simulation-first.
  - **Maze (rewritten 2026-09-23 from the owner's own AE1 report + slides, Dec 2025, and the repo's firmware):** Cornerstone of Engineering, a team of two. He did the Onshape CAD, all the printing and the MicroPython; his partner built the team's maze (for another team to run) and helped wire. It replaced the class kit (tall and tippy, a ping-pong-ball caster, no mounting): a printed **tank drive** of 2 geared DC motors with rubber-band treads, **1.6 in tall without the LCD** (about half the kit). Sensing is **2 ultrasonics angled outward plus a front IR**. The maze program is **proportional wall-centering** (error = left − right, gain 4, base 55%, both motors clamped to 20–100% duty). It does **no mapping, backtracking, pathfinding or line-following**; an earlier version of the page claimed all four, so don't bring them back. A potentiometer + LCD menu picks Forward / Preset Path / Maze, and asyncio keeps the LED and LCD animations from freezing the motors. It completed all three challenges. Fixes worth telling: crowned wheels (treads walked off), the front IR (it ran into walls), gain tuning, the knob menu (no re-flashing), and asyncio (the freezes). Carpet traction was never fixed. The report and slides live in the owner's Downloads; the page's photos came out of them.
  - **Combat:** Korybantes (3lb beater, top-35 US '25), Achilles metal (1lb, top-15 US '25, **single-cam shuffler on the newest version only** — walks flipped), Achilles plastic (4th nat'l, top-30 US '24, belt-driven undercutter).
- **Voice:** concrete and specific (real part names/numbers). No vague superlatives ("cutting-edge," "passionate," "innovative solutions").
- **No mid-sentence bold** (owner, 2026-09). Don't bold words or clauses inside running prose. Where a phrase genuinely needs emphasis, use `var(--signal)` red at normal weight, or a tone shift to `var(--text)` against `--text-secondary` body copy — not weight. Keep the `<strong>`/`<b>` element for screen readers and set `font-weight:inherit` on it, so the emphasis isn't colour-only for assistive tech. This is the one sanctioned use of the accent on prose; everything else in §2 still applies.

## 7. Images (workflow + traps)
- Source photos → `images/` (any name, spaces ok). **Optimize** into `images/web/<kebab-slug>.jpg` with **Pillow**:
  `ImageOps.exif_transpose(im)` **(critical — bakes phone rotation; System.Drawing does NOT do this and produced sideways images)** → flatten alpha onto white → `thumbnail((2000,2000), LANCZOS)` → save JPEG `quality=84, optimize, progressive`.
- Reference **only** `images/web/…` in the HTML. Add intrinsic `width`/`height` to every content `<img>` (prevents layout shift).
- **HEIC** can't be displayed by browsers — convert first. `pip install pillow-heif` then `pillow_heif.register_heif_opener()` works (installs cleanly, even on Python 3.14), after which the normal Pillow pipeline handles the `.HEIC` like any other source.
- **⚠ Encoding trap:** editing `index.html` via PowerShell `Get-Content … | WriteAllText` misreads UTF-8 as CP1252 → mojibake (`·`→`Â·`, `×`→`Ã—`). Use the **Edit tool** or **Python (utf-8)** for any programmatic HTML edit. Recovery if it happens: read bytes as UTF-8, re-encode to CP1252 (Python handles the 5 undefined bytes as identity).

## 8. Video
- `.video-spot` driven by `data-video`. **Prefer a YouTube/Vimeo link** (renders a lite facade: thumbnail + play → swaps to an inline `<iframe>` on click).
- Inline playback works only when the page is **served over http/https** (localhost or the deployed site). Opening the raw file (`file://`) can't embed YouTube (browser security). The local `.mov` files are **HEVC** (Safari/Edge only) — don't use them as `<video>` sources.

## 9. QA workflow — DO THIS before finishing any visual change
No browser extension is available; **drive a headless Chromium yourself**.
1. **Node v24 and npm are installed** (found 2026-09-22), so Playwright is possible, but the **Chrome that is already installed** needs nothing:
   `& "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --disable-gpu --hide-scrollbars --force-color-profile=srgb --screenshot="out.png" --window-size=1400,3400 --virtual-time-budget=9000 "http://127.0.0.1:8099/index.html#experience"`
   Serve the site first: `python -m http.server 8099` from the repo. Light theme: add `--blink-settings=preferredColorScheme=1`. Crop tall captures into readable sections with Pillow.
   - Add **`--force-prefers-reduced-motion`**, or the **count-ups get caught mid-animation** and a screenshot shows "6+ Members" where the source says `data-count="10"`. That looks like a content bug and isn't one.
   - Don't try to review desktop layouts through the in-app browser pane: it captures device pixels, so at a 1400px emulated viewport you only see a corner.
   - **`--window-size` widths below ~500px get clamped.** Chrome lays the page out wider and then crops the capture, which looks exactly like horizontal overflow — clipped images, text cut mid-word at the right edge. Don't chase it. Use ≥560 for a narrow visual check, and measure real mobile overflow by emulating a true 390 viewport and reading `documentElement.scrollWidth` vs `innerWidth`.
   - The pane's tab does no layout while hidden, so `loading="lazy"` images sit at `naturalWidth===0` forever and look broken. Confirm with `fetch()` + a fresh `new Image()` before believing a broken-image report.
   - **Better: drive Chrome over the DevTools protocol (2026-09).** That gives true phone viewports (`Emulation.setDeviceMetricsOverride` with `mobile:true`, so no ~500px clamp), reduced-motion and colour-scheme emulation (`Emulation.setEmulatedMedia`), in-page JS for audits, and clipped captures (`Page.captureScreenshot` with `clip` + `captureBeyondViewport`). It needs one pure-Python package, installed into a throwaway dir so the global Python isn't touched: `python -m pip install --target <scratch>/pylib websocket-client`. Launch Chrome with `--remote-debugging-port=N --remote-allow-origins=* --user-data-dir=<scratch>/prof-N`, then connect to the page target from `http://127.0.0.1:N/json`. Traps met doing it:
     - Call `Network.setCacheDisabled`, or edits don't show and you review stale CSS.
     - Use **one profile dir per port**. Two Chromes on one profile, or a profile left behind by a killed run, drops the socket mid-run ("Connection to remote host was lost").
     - Full-page captures beyond the viewport can show a large lazy image as an **empty grey box** (the Omar bleed looked broken; it wasn't). Set every `img.loading='eager'` and `await img.decode()` before capturing. Race the decode against a timeout: YouTube thumbnails never resolve while they're still lazy.
     - Force-load images for the **active tab only**. Decoding every photo on the site at once (roughly 700MB of bitmaps) crashes headless Chrome.
     - Clipboard tests need `Emulation.setFocusEmulationEnabled` plus `Browser.grantPermissions`, or `readText` fails with "Document is not focused".
     - Google Fonts occasionally fails a CORS check on a first load. If the console-error audit flags `fonts.gstatic.com`, rerun that view before assuming a bug.
     - Never stop `chrome.exe` by name: the owner's own browser runs under it. Match the headless ones on `--remote-debugging-port` in the command line.
     - **No installs at all (2026-09-23):** Node 22+ has a built-in `WebSocket`, so a plain `.mjs` script drives Chrome over DevTools without the `websocket-client` package above.
     - **Brief page count:** emulate print media and call `Page.printToPDF` with `preferCSSPageSize:true`, then count `/Type /Page` objects. The `--print-to-pdf` command-line flag hung on this machine. `tools/print-pdf.mjs` does all of this and reports the page count. The brief is US Letter, and each page should keep about half an inch spare, since other browsers set text a little differently.
     - **Résumé screening check (2026-09-23):** after any edit to `tools/resume.html`, every font in the PDF must be `/Type0` (a variable webfont embeds as `/Type3`, which older applicant-tracking parsers can't read), and pdftotext, pdfminer and pypdf must all return every heading, date and contact field intact. Headings carry no letter-spacing, because it split "EDUCATION" into letters. `tools/README.md` has the one-liners.
     - **Check first screens at real sizes** (1440×900, 390×844). Growing the viewport to the full page height to capture everything inflates anything sized in `vh`, e.g. the case-study hero figure's `max-height:76vh`.
     - **Test the default theme as a fresh visitor:** emulate `prefers-color-scheme: dark` without writing `localStorage`; the page must still land on light.
     - PowerShell 5.1 strips the quotes out of JSON passed as an argument to a native exe (`node`, `chrome`). Pass JSON through a file.
2. Screenshot **every route × {1440, 820, 390} × {dark, light}**. Before each screenshot: force-load lazy media (set `img.src` from `data-src`, set bg from `data-img`) and use context `reducedMotion:'reduce'` (so `.reveal` elements aren't hidden).
3. Auto-audit each view for: **horizontal overflow** (flag only elements exceeding the viewport that have **no clipping ancestor** — marquees/motifs are intentionally clipped, so raw `scrollWidth` is a false positive), **sub-12px text**, **clipped text**, **broken images** (`naturalWidth===0`), **console/page errors**.
4. Tall project pages render tiny at full-page scale — capture **element/section clips** at readable size (note: `element.screenshot()` can hang on `overflow:clip` or hidden-tab elements; scope to `.tab-content.active`, `waitForFunction` on image `naturalWidth>0`, and use `page.screenshot({clip})`).
5. A **contact sheet** (each image + its caption + its section) is the fastest way to verify every image is placed well and oriented correctly.
Ship only when the audit is clean across all views AND the screenshots look genuinely good.

## 10. Dev-mode editor (preserve)
How it works is in the comment at the top of `index.html`. If you rename/restructure text classes, update `TEXT_SELECTOR` in the dev-mode `<script>` so those blocks stay editable. It covers the foam-case hero text (`.sf-name`, `.sf-lede`, `.sf-labels .dymo`, `.sf-fact span`), which replaced the old `.hero-*` entries.

## 11. Git / deploy
- Deploys from `main`. Commit when asked; end commit messages with the `Co-Authored-By: Claude …` trailer.
- **Commit as the owner (2026-09-23).** The repo-local git identity is `ka1e27 <kylegtran2007@gmail.com>`, his choice, so commits count on his GitHub graph. Claude goes only in the `Co-Authored-By` trailer. Don't set the identity back to Claude, and don't rewrite the earlier Claude-authored history.
- Keep the 214 MB `images/Video of subtasks for dreams v1.mov` **gitignored**. Don't commit stray/unreferenced source files.
- Convention so far: separate commits for image additions vs. `index.html` changes.

## 12. Open items to confirm with the owner
- **Crimp Battle Board** — CONFIRMED (2026-07): a head-to-head crimp-strength game for climbers; two people pull on it to see who has the stronger crimp. Blurb on the Misc page updated accordingly.
- **Instagram tiles** use real robot photos (IG can't be scraped without auth). To show actual posts, the owner drops post images into `images/` and they get swapped in.
- **Podium counts (raised 2026-09; answered 2026-09-23).** The awards table is his own robots (4 firsts, 8 seconds, the 4th-place national) and the 16 is the club's across its 5 events. He confirmed the site's numbers are right. His robots' 8 seconds only looked like too many for the club's 7 because the table covers 10 competitions and the club's count covers 5. The résumé and the brief use the same numbers.
- **Link preview is stale (raised 2026-09-23, open).** `images/web/og-card.jpg` is the dark version, while light is now the default theme. Re-rendering it from `tools/og-card.html` in light would match what visitors see.
- **Rejected designs are on local-only branches:** `shadow-foam-preview` (the foam motif site-wide) and `redesign-2026` (a drawing-sheet prototype). Neither was pushed. Don't merge them; the owner can delete them.
- **Résumé PDF lagged the site (raised 2026-09; fixed 2026-09-23).** It had no Robotics minor, no graduation date and no portfolio link, and it still called the maze robot a pathfinder. It is now printed from `tools/resume.html`, so the site's sessions can keep it current.
- **"CNC machining" as a hands-on skill (raised 2026-09, answered 2026-09-23).** It appears in the marquee, the Capabilities grid, the brief and the résumé, while §6 says machining is outsourced. It's accurate if it means designing for CNC and sending parts out. If an interviewer asks "which mill?", it isn't. The About copy was already changed to "send them out to be machined". Answered 2026-09-23: he has run 2.5D CNC himself, and also sent machining and heat treating out. The résumé and brief say "2.5D CNC machining"; the site's "CNC machining" is accurate. He starts learning a Tormach mill around 2026-09-30 to machine the cycloidal gearbox and the arm. Once he has used it, add it to the skills, and ask whether the arm page's manufacturing route changes (the disc manufacturing route is still open there, and the tolerance section weighs waterjet cutting).
- **Maze page loose ends (raised 2026-09-23, open).** (1) In the repo's `motor_control.py` the maze loop reads the front IR sensor (`current_front`) and never uses it, so the page says only that it was added to detect walls ahead, not what it does. If a later version acted on it, it should go to GitHub. (2) The partner is unnamed on the site; the slides name him. Ask before crediting him by name. (3) The slides embed two more run videos (YouTube `sGzrKsEa0Qc`, `sH4t-jbIK8c`, both titled "December 4, 2025"). They are dim home tests and weren't used. (4) The slides use a Northeastern University London template. If that's where he took the course, it may be worth saying; ask.
